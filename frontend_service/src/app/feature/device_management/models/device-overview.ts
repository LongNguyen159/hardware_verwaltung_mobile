export interface DeviceInput {
    deviceName: string
    deviceLocation: string
}

export interface DeviceMetaData {
    id: number,
    deviceName: string,
    location: string,
    inLage: string,
    duration: string
}